# Example environment variables for A³X
# Copy this file to .env and fill in your actual values.

# API Key for Tavily Search (required if TAVILY_ENABLED=True in core/config.py)
TAVILY_API_KEY="YOUR_TAVILY_API_KEY_HERE"

# Other potential variables could go here in the future (e.g., LLM Server URL if made configurable)
